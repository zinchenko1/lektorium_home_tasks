{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>
    <a class="btn btn-success" href="{{ path('student_edit', {'id': student.id}) }}">edit</a>
    <a class="btn btn-warning" href="{{ path('student_index') }}">back to list</a>

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ student.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ student.lastName }}</td>
            </tr>
        </tbody>
    </table>

    {{ include('student/_delete_form.html.twig') }}
{% endblock %}
